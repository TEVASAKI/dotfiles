# Expand the tab tilde
set expand-tilde  on

# Tab completion at the time,
# I ignore upper and lower case letters.
set completion-ignore-case  on

# Japanese relationship
set convert-meta  off
set meta-flag     on
set output-meta   on

